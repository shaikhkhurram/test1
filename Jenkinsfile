pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Example: run Maven build (uncomment if you have Maven)
                // sh 'mvn clean compile'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                // Example: run Maven tests
                // sh 'mvn test'
            }
        }
        stage('Package') {
            steps {
                echo 'Packaging...'
                // Example: package the app
                // sh 'mvn package'
            }
        }
    }
}
